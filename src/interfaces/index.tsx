export interface InterInitialState {
  filterVal: string;
}
export interface InterSubmit {
  id: number | string;
  name: string;
  year: number;
  color: string;
  pantone_value: string;
}
export interface InterPostData {
  id: number;
  name: string;
  year: number;
  color: string;
  pantone_value: string;
}
